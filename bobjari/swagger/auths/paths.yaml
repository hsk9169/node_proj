tags:
    name: auths
    description: auths API

paths:
    /auths/kakao:
        get:
            summary: start login with kakao
            tags:
                - auths
            responses:
                200:
                    description: kakao login success, got user profile
                    content:
                        application/json:
                            $ref: '#/components/schemas/kakaoProfile'